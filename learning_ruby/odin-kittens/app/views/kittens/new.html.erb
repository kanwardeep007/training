<h1>New Kitten</h1>

<%= form_for (@kitten) do |k| %>
	
	<%= render partial: 'form_kitten', locals: {kitten: k}  %>
	<%= k.submit "Create kitten" %>

<% end %>
<%= link_to "Home Page" , kittens_path %>